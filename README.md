# Modal Window

A simple modal window implementation using vanilla JavaScript, HTML, and CSS.

## Features

- Click any button to open the modal
- Close modal by:
  - Clicking the × button
  - Clicking the overlay (background)
  - Pressing the Escape key

## Usage

Open `index.html` in your browser.

## Technologies

- HTML5
- CSS3 (Flexbox, backdrop-filter)
- Vanilla JavaScript (DOM manipulation, event handling)
